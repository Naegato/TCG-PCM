// Shared between the component that owns an animation and GameContext.tsx, which paces
// one-by-one event playback. Keep these in sync with app/globals.css.
export const CARD_TRIGGERED_SHAKE_DURATION = 500;
export const CARD_MARKER_DURATION = 900;
export const CONSUMABLE_REVEAL_DURATION_MS = 1200;
